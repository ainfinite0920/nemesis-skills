# 标签 (Tag) 实体

## 定义

标签用于对提示词进行多维度标记。

## 属性

| 属性 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | string | 标签名称 |
| color | string | 标签颜色（十六进制） |
| created_at | datetime | 创建时间 |

## 业务规则

- name 长度限制：最大 50 字符
- 标签名称全局唯一
- 颜色格式：#RRGGBB
- 支持多对多关联到提示词